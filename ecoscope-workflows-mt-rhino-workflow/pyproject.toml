# AUTOGENERATED BY ECOSCOPE-WORKFLOWS; see fingerprint in README.md for details

[build-system]
requires = ["hatchling>=1.18.0,<2", "ruamel.yaml>=0.17.0,<1"]
build-backend = "hatchling.build"

[project]
name = "ecoscope-workflows-mt-rhino-workflow"
dynamic = ["version"]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ecoscope-workflows-mt-rhino-workflow = "ecoscope_workflows_mt_rhino_workflow.cli:cli"

[tool.hatch.metadata.hooks.custom]
path = "hatch_build.py"

[tool.hatch.build.targets.wheel]
packages = ["ecoscope_workflows_mt_rhino_workflow"]
